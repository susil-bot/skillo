# Skillo Social Connector – copy to .env and fill in values
# Base URL where this server is reachable (for OAuth redirect_uri)
BASE_URL=http://localhost:4000
PORT=4000

# --- Meta (Facebook + Instagram Business / Creator) ---
# Create app at https://developers.facebook.com → Facebook Login + Instagram Graph API
# Add redirect URI: {BASE_URL}/auth/meta/callback
META_APP_ID=
META_APP_SECRET=
# Optional; defaults to {BASE_URL}/auth/meta/callback
# META_REDIRECT_URI=http://localhost:4000/auth/meta/callback

# --- LinkedIn ---
# Create app at https://www.linkedin.com/developers → Auth → redirect URL: {BASE_URL}/auth/linkedin/callback
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
# Optional
# LINKEDIN_REDIRECT_URI=http://localhost:4000/auth/linkedin/callback

# --- YouTube (Google) ---
# Create OAuth 2.0 credentials at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: {BASE_URL}/auth/youtube/callback
# Enable YouTube Data API v3 and YouTube Analytics API
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional
# GOOGLE_REDIRECT_URI=http://localhost:4000/auth/youtube/callback
